# Default deny all ingress and egress in petclinic namespace
# All allowed traffic must be explicitly permitted below
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: petclinic
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
