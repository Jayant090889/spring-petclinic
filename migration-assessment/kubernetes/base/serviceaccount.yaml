apiVersion: v1
kind: ServiceAccount
metadata:
  name: petclinic-sa
  namespace: petclinic
  labels:
    app: petclinic
  annotations:
    # AKS workload identity annotation (populated during AKS migration)
    # azure.workload.identity/client-id: "<managed-identity-client-id>"
