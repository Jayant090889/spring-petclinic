# TEMPLATE ONLY — never commit real values
# Populate via:
#   kubectl create secret generic petclinic-db-secret \
#     --from-literal=username=petclinic \
#     --from-literal=password=<from-keyvault> \
#     -n petclinic
#
# AKS production: use Azure Key Vault + Secret Store CSI Driver instead
apiVersion: v1
kind: Secret
metadata:
  name: petclinic-db-secret
  namespace: petclinic
  annotations:
    # AKS: this secret is synced from Azure Key Vault via CSI driver
    # azure.keyvault.secret: "petclinic-db-creds"
type: Opaque
data:
  username: <base64-encoded-username>   # echo -n 'petclinic' | base64
  password: <base64-encoded-password>   # echo -n 'yourpassword' | base64
